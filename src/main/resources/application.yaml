spring:
  application:
    name: bank.card
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
cors:
  url:
  port:
jwt:
  lifetime:
  secret: